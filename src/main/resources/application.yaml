spring:
    datasource:
        url: jdbc:mysql://localhost:3306/impulse
        username: root
        password: root
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
                ddl-auto: update
                format_sql: true
        show-sql: true
        defer-datasource-initialization: true
server:
    port: 3306
    servlet:
        context-path: /security